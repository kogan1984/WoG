#services:
#  app:
#    build: .
#    command: flask MainScores --debug run --host=0.0.0.0 --port=5000
#    ports:
#      - "5000:5000"
#    volumes:
#      - .:/app

# docker-compose.yaml
version: '3'

services:
  web:
    build: '.'
    ports:
      - '5000:5000'